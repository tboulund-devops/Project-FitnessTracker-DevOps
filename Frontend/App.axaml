<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Frontend.App"
             xmlns:viewModels="clr-namespace:FitnessTracker.UI.ViewModels"
             xmlns:views="clr-namespace:Frontend.Views"
             xmlns:viewModels1="clr-namespace:Frontend.ViewModels"
             RequestedThemeVariant="Default">
    
    <!-- "Default" ThemeVariant follows system theme variant. "Dark" or "Light" are other available options. -->

             <Application.DataTemplates>
                 <DataTemplate DataType="viewModels1:LoginPageVM">
                     <views:LoginPage/>
                 </DataTemplate>
                 
                 <DataTemplate DataType="viewModels:HomePageVM">
                     <views:HomePage/>
                 </DataTemplate>
             </Application.DataTemplates>

    <Application.Styles>
        <FluentTheme />
    </Application.Styles>
</Application>